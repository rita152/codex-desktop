# Steering docs (.kiro/steering/)

## Scope

Applies to `.kiro/steering/*`.

## Overview

Project-specific engineering rules and review checklists. Treat these as policy docs that should match the actual codebase.

## Files

- `frontend-rules.md`: frontend layering, Storybook rules, styling (CSS variables), invoke boundary.
- `frontend-checklist.md`: merge/PR acceptance checklist.
- `structure.md`: repo structure and Tauri command wiring conventions.
- `tech.md`: stack + TS strictness expectations.
- `product.md`: product overview (minimal engineering constraints).

## Notes

- These documents may include fileMatch metadata for tooling; keep it in sync with repo layout.
